name: Tests & Release

on:
  push:
    branches: [main, develop]
    tags: ['v*']
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Test on Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.8', '3.9', '3.10', '3.11', '3.12', '3.13']
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      
      - name: Check Python version
        run: |
          python --version
          python -m venv --help
      
      - name: Check syntax
        run: python -m py_compile py
      
      - name: Run basic tests
        run: |
          mkdir /tmp/test_py_ci
          cd /tmp/test_py_ci
          python ${{ github.workspace }}/py init
          python ${{ github.workspace }}/py add requests
          python ${{ github.workspace }}/py list
          python ${{ github.workspace }}/py doctor
          python ${{ github.workspace }}/py check
          python ${{ github.workspace }}/py cache info

  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install tools
        run: pip install pylint black flake8
      
      - name: Black format check
        run: black --check py || true
      
      - name: Flake8 lint
        run: flake8 py --max-line-length=120 --exclude=.venv || true
      
      - name: Pylint
        run: pylint py --disable=all --enable=syntax-error || true

  release:
    name: Create Release & Publish to PyPI
    needs: [test, lint]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    permissions:
      contents: write
      packages: write
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
      
      - name: Build distribution
        run: |
          pip install build
          python -m build
      
      - name: Create GitHub Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: py ${{ steps.version.outputs.VERSION }}
          body: |
            # py v${{ steps.version.outputs.VERSION }}
            
            See CHANGELOG.md for details.
            
            ## Installation
            
            ### One-liner:
            ```bash
            curl https://raw.githubusercontent.com/github/py/main/install.sh | bash
            ```
            
            ### Or via pip:
            ```bash
            pip install py-tool
            ```
          draft: false
          prerelease: false
      
      - name: Upload to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          pip install twine
          twine upload dist/* --skip-existing

  docs:
    name: Build & Deploy Documentation
    runs-on: ubuntu-latest
    if: github.push && github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Check documentation exists
        run: |
          ls -la README.md COMMAND_REFERENCE.md ARCHITECTURE.md
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          include_dotfiles: true
